{
  "name": "mcp-server-sonix",
  "version": "1.0.0",
  "description": "MCP server for Sonix.ai transcription service",
  "type": "module",
  "bin": {
    "mcp-server-sonix": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc && chmod +x dist/index.js",
    "watch": "tsc --watch",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "express": "^5.2.1",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^22.0.0",
    "typescript": "^5.8.0"
  }
}
